
<h1>Categoría <%= @category.nombre %></h1>



<ul>
  <li> <%= link_to "Edit", edit_category_path(@category) %> </li>
  <li> <%= link_to "Destroy", category_path(@category), data: {
    turbo_method: :delete,
    turbo_confirm: "Estás seguro de eliminar "+@category.nombre+"?"
  } %> </li>
</ul>
